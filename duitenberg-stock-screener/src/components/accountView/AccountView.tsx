import React from "react";

/*
This should be what App.tsx uses, so this should have everything. More components will probably
be needed, but the design isn't done yet. Make more as needed.
 */
function AccountView() {
    return (
        <></>
    )
}

export default AccountView;